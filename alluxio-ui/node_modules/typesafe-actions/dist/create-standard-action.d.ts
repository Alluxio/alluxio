import { StringType, Box, FsaBuilder, FsaMapBuilder } from './types';
export interface CreateStandardAction<T extends StringType> {
    <P = void, M = void>(): FsaBuilder<T, Box<P>, Box<M>>;
    map<R, P = void, M = void>(fn: (payload: P, meta: M) => R): FsaMapBuilder<T, Box<R>, Box<P>, Box<M>>;
}
export declare function createStandardAction<T extends StringType>(actionType: T): CreateStandardAction<T>;
