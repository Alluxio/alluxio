{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "3.10.7"
  },
  "engines": {
    "node": "10.11.0",
    "npm": "6.4.1"
  },
  "scripts": {
    "start": "lerna run start --parallel --stream",
    "build": "lerna run build --parallel --stream",
    "test-ci": "lerna run test-ci --parallel --stream",
    "coverage-ci": "lerna run coverage-ci --parallel --stream"
  }
}
