- name: download glusterfs
  shell: sudo wget -q http://download.gluster.org/pub/gluster/glusterfs/3.5/LATEST/CentOS/glusterfs-epel.repo -O /etc/yum.repos.d/glusterfs-epel.repo && \
         sudo yum install -q -y glusterfs-server glusterfs-client

# vim :set filetype=ansible.yaml:
