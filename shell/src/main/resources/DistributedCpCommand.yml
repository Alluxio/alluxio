name: distributedCp
usage: distributedCp <src> <dst>
description: |
  The `distributedCp` command copies a file or directory in parallel at file level.
examples: |
  ```console
  $ ./bin/alluxio fs distributedCp /hdfs/file1 /s3/
  ```