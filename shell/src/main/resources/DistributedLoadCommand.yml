name: distributedLoad
usage: distributedLoad [--replication <num>] <path>
description: |
  The `distributedLoad` command loads a file or directory in Alluxio space, making it resident in memory.
examples: |
  ```console
  $ ./bin/alluxio fs distributedLoad /file1
  ```
